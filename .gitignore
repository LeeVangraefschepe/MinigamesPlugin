target/
*.class

# The local test server directory is entirely regenerated by
# build-spigot.sh / start-test-server.sh (jars, bundler libraries, world
# data, logs, server.properties, whitelist/ops/ban lists, etc).
server/*
!server/.gitkeep
